# cluster-service/src/main/resources/application.properties
# NOTE: Cluster Service owns NO PostgreSQL table — Redis only.

spring.application.name=cluster-service
server.port=${SERVER_PORT:8083}

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
app.kafka.topics.cluster-events=${CLUSTER_EVENTS_TOPIC:cluster-events}

management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always

logging.level.com.distributed.kvstore=DEBUG




# reads SERVER_PORT from env, defaults to 8081


# reads NODE_ID from env, defaults to node-1
app.node-id=${NODE_ID:node-1}